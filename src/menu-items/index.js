// project import
import dashboard from './dashboard';
import user from './user';
import trip from './trip';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [dashboard, user, trip]
};

export default menuItems;
